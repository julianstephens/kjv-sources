# v1.0.0

- adds `kjvcorpus` package for loading and querying the KJV corpus
- adds `ingest` tool for processing raw HTML chapter files into structured JSON format
- adds `extract` tool for generating canonical book metadata and chapter file mappings from raw sources
- adds `verify` tool for validating the integrity of the processed corpus against expected metadata
